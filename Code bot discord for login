//Code bot discord for login




const {  Client, GatewayIntentBite, GatewayIntentBits } = require("discord.js");

const client = new Client({
    intents: [
        GatewayIntentBits.Guilds 
    ]
});

client.on("ready", () => {
    console.log("operational bot");
});

client.login("YOUR TOKEN");
